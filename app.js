const feed=document.getElementById('feed');const input=document.getElementById('postInput');const postBtn=document.getElementById('postBtn');const imageInput=document.getElementById('imageInput');const photoBtn=document.getElementById('photoBtn');const createNav=document.getElementById('createNav');let posts=JSON.parse(localStorage.getItem('mada_posts')||'[]');let selectedImage='';
function save(){localStorage.setItem('mada_posts',JSON.stringify(posts))}
function render(){feed.innerHTML='';if(!posts.length){feed.innerHTML='<div class="card empty">أول منشور في Mada سيكون من نصيبك 👋</div>';return}posts.forEach((p,i)=>{const el=document.createElement('article');el.className='card post';el.innerHTML=`<div class="post-head"><div class="avatar">أ</div><div><div class="post-name">مستخدم Mada</div><div class="post-time">الآن</div></div></div><div class="post-text"></div>${p.image?`<img class="post-image" src="${p.image}" alt="صورة المنشور">`:''}<div class="post-actions"><button class="like ${p.liked?'liked':''}" data-i="${i}">👍 إعجاب ${p.likes||0}</button><button>💬 تعليق</button><button>↗️ مشاركة</button></div>`;el.querySelector('.post-text').textContent=p.text;feed.appendChild(el)})}
function addPost(){const text=input.value.trim();if(!text&&!selectedImage)return;posts.unshift({text,image:selectedImage,likes:0,liked:false});save();input.value='';selectedImage='';imageInput.value='';render()}
postBtn.onclick=addPost;input.addEventListener('keydown',e=>{if(e.key==='Enter')addPost()});photoBtn.onclick=()=>imageInput.click();createNav.onclick=()=>input.focus();imageInput.onchange=()=>{const f=imageInput.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{selectedImage=r.result;input.placeholder='اكتب وصف الصورة ثم اضغط نشر'};r.readAsDataURL(f)};feed.onclick=e=>{const b=e.target.closest('.like');if(!b)return;const i=Number(b.dataset.i);posts[i].liked=!posts[i].liked;posts[i].likes=(posts[i].likes||0)+(posts[i].liked?1:-1);save();render()};render();