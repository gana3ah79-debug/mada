(function(){
 const sb=()=>window.MADA_SUPABASE_CLIENT||window.supabase.createClient(window.MADA_SUPABASE_URL,window.MADA_SUPABASE_KEY);const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
 window.MadaVerification={badge:p=>p?.is_verified?`<span class="mada-verified ${p.verification_type==='premium'?'premium':''}" title="${p.verification_type==='premium'?'توثيق Premium':'توثيق مجاني'}">${p.verification_type==='premium'?'💎':'✓'}</span>`:'',async open(){const u=(await sb().auth.getUser()).data.user;if(!u)return;const s=(await sb().from('verification_settings').select('*').eq('id',true).maybeSingle()).data;if(!s)return;const p=(await sb().from('profiles').select('*').eq('id',u.id).single()).data;const r=await sb().rpc('refresh_user_verification',{target_user:u.id});const d=r.data||{};showModal('✓ التوثيق',`<div class="verification-box"><h3>التوثيق المجاني</h3><p>${esc(s.free_member_text)}</p><div>الحساب: ${d.reasons?.includes('account_age')?'❌ لم يكمل المدة':'✅'}</div><div>الملف الشخصي: ${d.reasons?.includes('profile_complete')?'❌ أكمل الصورة والاسم والنبذة':'✅'}</div><div>المنشورات: ${d.posts||0} / ${s.free_posts_required} ${d.reasons?.includes('posts')?'❌':'✅'}</div><div>التفاعلات المستلمة: ${d.reactions||0} / ${s.free_reactions_required} ${d.reasons?.includes('reactions')?'❌':'✅'}</div><div>الأصدقاء: ${d.friends||0} / ${s.free_friends_required} ${d.reasons?.includes('friends')?'❌':'✅'}</div><hr><h3>💎 توثيق Premium</h3>${s.premium_enabled?`<p>السعر: ${s.premium_price_egp} جنيه — المدة: ${s.premium_duration_months} شهر</p><p>${esc(s.premium_instructions||'سيتم تفعيل طلب التوثيق Premium قريبًا.')}</p>`:'<p class="muted">توثيق Premium غير متاح حاليًا.</p>'}${p.is_verified?`<p class="verified-ok">${p.verification_type==='premium'?'💎 حسابك موثق Premium':'✓ حسابك موثق مجانًا'}</p>`:''}</div>`)}};
 const old=window.loadFeed; if(old){window.loadFeed=async function(){await old();document.querySelectorAll('.post-name').forEach(async el=>{const id=el.dataset.profile;if(!id)return;const p=(await sb().from('profiles').select('is_verified,verification_type').eq('id',id).maybeSingle()).data;if(p?.is_verified&&!el.querySelector('.mada-verified'))el.insertAdjacentHTML('beforeend',' '+MadaVerification.badge(p));})}};
})();