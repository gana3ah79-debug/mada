/* Mada comment/reply reactions v2 — fixes optimistic/new comments + replies and keeps one reaction per user. */
(function(){
'use strict';
if(window.__MADA_COMMENT_REACTIONS_V2)return;window.__MADA_COMMENT_REACTIONS_V2=true;
const R={like:['👍','إعجاب'],love:['❤️','حب'],haha:['😂','ضحك'],wow:['😮','واو'],sad:['😢','حزن'],angry:['😡','غضب']};
const sb=()=>window.MADA_SUPABASE_CLIENT||window.sb;const me=()=>window.madaUser?.()||window.user||null;
const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
function idOf(row){return row?.dataset?.commentId||row?.getAttribute?.('data-comment-id')||null}
function addUI(row){const id=idOf(row);if(!id||row.dataset.madaRxV2)return;row.dataset.madaRxV2='1';let wrap=row.querySelector('.mada-comment-reaction-wrap');if(!wrap){wrap=document.createElement('div');wrap.className='mada-comment-reaction-wrap';wrap.innerHTML='<button type="button" class="mada-comment-reaction" aria-label="التفاعلات" title="التفاعلات"><span>👍</span><b>0</b></button>';row.appendChild(wrap)}const btn=wrap.querySelector('.mada-comment-reaction');btn.dataset.commentId=id;load(row,id)}
async function load(row,id){const s=sb(),u=me();if(!s)return;const q=await s.from('comment_reactions').select('user_id,reaction_type').eq('comment_id',id);if(q.error)return;const rows=q.data||[],mine=u?rows.find(x=>x.user_id===u.id):null,b=row.querySelector('.mada-comment-reaction');if(!b)return;b.querySelector('b').textContent=rows.length;b.querySelector('span').textContent=mine?(R[mine.reaction_type]?.[0]||'👍'):'👍';b.dataset.type=mine?.reaction_type||'';b.classList.toggle('mada-reacted',!!mine)}
function picker(row,btn){document.querySelectorAll('.mada-comment-rx-picker-v2').forEach(x=>x.remove());const p=document.createElement('div');p.className='mada-comment-rx-picker-v2';Object.entries(R).forEach(([type,v])=>{const b=document.createElement('button');b.type='button';b.textContent=v[0];b.title=v[1];b.onclick=e=>{e.stopPropagation();save(row,btn,type);p.remove()};p.appendChild(b)});document.body.appendChild(p);const r=btn.getBoundingClientRect(),w=p.getBoundingClientRect();p.style.left=Math.max(6,Math.min(innerWidth-w.width-6,r.left+r.width/2-w.width/2))+'px';p.style.top=Math.max(6,r.top-w.height-8)+'px'}
async function save(row,btn,type){const s=sb(),u=me(),id=idOf(row);if(!s||!u||!id){if(!u)alert('سجّل الدخول أولاً.');return}const q=await s.from('comment_reactions').select('reaction_type').eq('comment_id',id).eq('user_id',u.id).maybeSingle();if(q.error){alert('تعذر قراءة التفاعل: '+q.error.message);return}const current=q.data?.reaction_type||'',next=current===type?'':type;btn.dataset.type=next;btn.querySelector('span').textContent=next?(R[next]?.[0]||'👍'):'👍';btn.classList.toggle('mada-reacted',!!next);let action;if(!next)action=s.from('comment_reactions').delete().eq('comment_id',id).eq('user_id',u.id);else if(current)action=s.from('comment_reactions').update({reaction_type:next}).eq('comment_id',id).eq('user_id',u.id);else action=s.from('comment_reactions').insert({comment_id:id,user_id:u.id,reaction_type:next});const r=await action;if(r.error){await load(row,id);alert('تعذر حفظ التفاعل: '+r.error.message);return}await load(row,id)}
function click(e){const b=e.target.closest('.mada-comment-reaction');if(!b)return;const row=b.closest('[data-comment-id]');if(!row)return;e.preventDefault();e.stopPropagation();if(b.dataset.type)save(row,b,b.dataset.type);else picker(row,b)}
function scan(){document.querySelectorAll('#feed [data-comment-id]').forEach(addUI)}
function style(){if(document.getElementById('mada-comment-reactions-v2-style'))return;const s=document.createElement('style');s.id='mada-comment-reactions-v2-style';s.textContent='.mada-comment-reaction-wrap{margin-inline-start:auto;display:flex;align-items:center}.mada-comment-reaction{border:0;background:transparent;border-radius:14px;padding:3px 7px;display:inline-flex;align-items:center;gap:3px;font-size:13px;cursor:pointer}.mada-comment-reaction b{font-size:11px;color:#6b7280}.mada-comment-reaction.mada-reacted{font-weight:800}.mada-comment-rx-picker-v2{position:fixed;z-index:99999;display:flex;gap:2px;padding:5px 6px;border-radius:16px;background:#fff;box-shadow:0 8px 25px rgba(0,0,0,.18);direction:ltr}.mada-comment-rx-picker-v2 button{border:0;background:transparent;font-size:20px;width:30px;height:30px;border-radius:50%;cursor:pointer}.mada-comment-rx-picker-v2 button:hover{background:#f1f5f9;transform:translateY(-2px)}';document.head.appendChild(s)}
function boot(){style();scan();const feed=document.getElementById('feed');if(feed){let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(scan,80)}).observe(feed,{childList:true,subtree:true})}document.addEventListener('click',click,true);document.addEventListener('click',e=>{if(!e.target.closest('.mada-comment-rx-picker-v2'))document.querySelectorAll('.mada-comment-rx-picker-v2').forEach(x=>x.remove())})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.MadaCommentReactionsV2={scan,save};
})();