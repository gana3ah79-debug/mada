/* Mada Reply Panel v2 - one dynamic comments/replies list with per-item reactions. */
(function(){
'use strict';
const $=(s,r=document)=>r.querySelector(s);const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const emojis=['😀','😂','😍','🥰','😘','😎','😭','😡','😮','😢','👏','🔥','❤️','👍','🙏','🎉','💯','✨'];
let postId=null,rootEl=null;
function ensure(){if($('#madaReplyPanel'))return;document.body.insertAdjacentHTML('beforeend',`<div id="madaReplyPanel" class="mada-reply-panel" hidden><div class="mada-reply-backdrop" data-rp-close></div><section class="mada-reply-sheet" role="dialog" aria-modal="true"><header><button type="button" data-rp-close>×</button><div><b>التعليقات والردود</b><small id="madaReplySub">كل التعليقات والردود في قائمة واحدة</small></div></header><div id="madaReplyPost"></div><main id="madaReplyList"></main><footer><button id="madaReplyEmoji" type="button">😊</button><input id="madaReplyInput" placeholder="اكتب تعليقًا أو ردًا…" autocomplete="off"><button id="madaReplySend" type="button">إرسال</button><div id="madaReplyPicker" hidden>${emojis.map(x=>`<button type="button" data-emoji="${x}">${x}</button>`).join('')}</div></footer></section></div>`);bind();}
function bind(){const p=$('#madaReplyPanel');p.addEventListener('click',e=>{if(e.target.closest('[data-rp-close]'))close();const b=e.target.closest('[data-reply-to]');if(b){const input=$('#madaReplyInput');input.dataset.parentId=b.dataset.replyTo;input.placeholder='الرد على التعليق…';input.focus()}const like=e.target.closest('[data-rp-react]');if(like){const active=like.classList.toggle('active');like.textContent=active?'♥':'♡';like.dataset.count=String(Math.max(0,(parseInt(like.dataset.count||'0',10)||0)+(active?1:-1)));like.title=`إعجاب ${like.dataset.count}`}});$('#madaReplyEmoji').onclick=()=>$('#madaReplyPicker').hidden=!$('#madaReplyPicker').hidden;$('#madaReplyPicker').onclick=e=>{const b=e.target.closest('[data-emoji]');if(b){$('#madaReplyInput').value+=b.dataset.emoji;$('#madaReplyInput').focus();$('#madaReplyPicker').hidden=true}};$('#madaReplySend').onclick=send;$('#madaReplyInput').onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}};}
function open(article){ensure();rootEl=article;postId=article?.id?.replace(/^post-/,'');if(!postId)return;$('#madaReplyPost').innerHTML=article.querySelector('.post-content')?.outerHTML||article.querySelector('.post-body')?.outerHTML||'';$('#madaReplyPanel').hidden=false;document.body.classList.add('mada-reply-open');loadAll();setTimeout(()=>$('#madaReplyInput').focus(),80);}
function close(){const p=$('#madaReplyPanel');if(p)p.hidden=true;document.body.classList.remove('mada-reply-open');}
async function loadAll(){const list=$('#madaReplyList'),sb=window.sb;list.innerHTML='<div class="mada-rp-loading">جاري تحميل التعليقات والردود…</div>';if(!sb||!postId){list.innerHTML='<div class="mada-rp-empty">سجّل الدخول لعرض التعليقات.</div>';return}const r=await sb.from('comments').select('id,author_id,body,created_at,parent_id').eq('post_id',postId).order('created_at',{ascending:true}).limit(300);if(r.error){list.innerHTML='<div class="mada-rp-empty">تعذر تحميل التعليقات.</div>';return}const rows=r.data||[],ids=[...new Set(rows.map(x=>x.author_id).filter(Boolean))];let names={};if(ids.length){const p=await sb.from('profiles').select('id,display_name').in('id',ids);(p.data||[]).forEach(x=>names[x.id]=x.display_name||'مستخدم')}const children={};rows.forEach(c=>{if(c.parent_id)(children[c.parent_id]??=[]).push(c)});const roots=rows.filter(c=>!c.parent_id);const render=(c,level=0)=>row(c,names[c.author_id]||'مستخدم',level)+(children[c.id]||[]).map(x=>render(x,level+1)).join('');list.innerHTML=roots.length?roots.map(c=>render(c)).join(''):'<div class="mada-rp-empty">لا توجد تعليقات بعد. كن أول من يعلق 👋</div>';}
function row(c,name,level){return `<article class="mada-rp-comment ${level?'mada-rp-reply':''}" style="--reply-level:${Math.min(level,4)}" data-comment-id="${esc(c.id)}"><div class="mada-rp-avatar">${esc(name.trim().charAt(0)||'م')}</div><div class="mada-rp-main"><div class="mada-rp-bubble"><b>${esc(name)}</b><p>${esc(c.body)}</p></div><small>${new Date(c.created_at).toLocaleString('ar-EG')}</small><div class="mada-rp-actions"><button type="button" data-reply-to="${esc(c.id)}">↩ رد</button><button type="button" data-rp-react data-count="0">♡ <span class="rp-like-count">0</span></button><button type="button" data-rp-copy="${esc(c.body)}">نسخ</button></div></div></article>`}
async function send(){const input=$('#madaReplyInput'),body=input.value.trim(),u=window.user,sb=window.sb;if(!body)return;if(!u||!sb){alert('سجّل الدخول أولاً.');return}const parent=input.dataset.parentId||null,btn=$('#madaReplySend');btn.disabled=true;btn.textContent='…';const r=await sb.from('comments').insert({post_id:postId,author_id:u.id,body,parent_id:parent}).select('id,created_at').single();btn.disabled=false;btn.textContent='إرسال';if(r.error){alert('تعذر إضافة الرد: '+r.error.message);return}input.value='';delete input.dataset.parentId;input.placeholder='اكتب تعليقًا أو ردًا…';await loadAll();}
document.addEventListener('click',e=>{const a=e.target.closest('[data-comments-open], [data-comment-toggle]');if(a){const article=a.closest('article.post');if(article){e.preventDefault();e.stopPropagation();open(article)}}const copy=e.target.closest('[data-rp-copy]');if(copy)navigator.clipboard?.writeText(copy.dataset.rpCopy);},true);
function boot(){ensure();document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.MadaReplyPanel={open,close,loadAll};
})();
