/* Mada comments v2 — comments only, no post preview, paginated and mobile-safe. */
(()=>{'use strict';
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const postId=new URLSearchParams(location.search).get('post'),PAGE=20,REPLY_PAGE=20;let sb,user,ownerId=null,offset=0,busy=false;
const RX={like:['👍','أعجبني'],love:['❤️','أحببته'],haha:['😂','مضحك'],wow:['😮','واو'],sad:['😢','حزين'],angry:['😡','غاضب']};
const uid=x=>x?.author_id||x?.user_id||null,name=p=>p?.display_name||p?.name||p?.username||'مستخدم',initial=n=>(String(n||'م').trim()[0]||'م').toUpperCase();
const time=v=>{try{const n=Date.now()-new Date(v).getTime();if(n<60000)return'الآن';if(n<3600000)return`منذ ${Math.max(1,Math.floor(n/60000))} د`;if(n<86400000)return`منذ ${Math.max(1,Math.floor(n/3600000))} س`;return new Date(v).toLocaleDateString('ar-EG',{day:'numeric',month:'short'})}catch{return''}};
function toast(t){const e=$('toast');if(!e)return;e.textContent=t;e.hidden=false;clearTimeout(window.__madaToast);window.__madaToast=setTimeout(()=>e.hidden=true,2200)}
async function profiles(ids){ids=[...new Set(ids.filter(Boolean))];if(!ids.length)return new Map();const r=await sb.from('profiles').select('id,display_name,username,avatar_url,is_verified').in('id',ids);return new Map((r.data||[]).map(x=>[x.id,x]))}
function avatar(p,cls='avatar'){return p?.avatar_url?`<img class="${cls}" src="${esc(p.avatar_url)}" alt="" loading="lazy">`:`<span class="${cls}">${esc(initial(name(p)))}</span>`}
async function loadOwner(){const r=await sb.from('posts').select('author_id').eq('id',postId).maybeSingle();if(!r.error)ownerId=r.data?.author_id||null}
async function reactionData(ids){ids=[...new Set(ids)];if(!ids.length)return new Map();const r=await sb.from('comment_reactions').select('comment_id,user_id,reaction_type').in('comment_id',ids);const m=new Map();(r.data||[]).forEach(x=>{if(!m.has(x.comment_id))m.set(x.comment_id,{counts:{},mine:null});const d=m.get(x.comment_id);d.counts[x.reaction_type]=(d.counts[x.reaction_type]||0)+1;if(user&&x.user_id===user.id)d.mine=x.reaction_type});return m}
function reactionSummary(d){return Object.entries(d?.counts||{}).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([k])=>RX[k]?.[0]||'👍').join('')}
function html(c,p,rd,reply=false){const id=esc(c.id),author=uid(c),d=rd.get(c.id)||{counts:{},mine:null},mine=d.mine,total=Object.values(d.counts).reduce((a,b)=>a+b,0),own=!!user&&author===user.id,canDelete=own||!!(user&&ownerId===user.id);return `<article class="comment ${reply?'reply':''}" data-id="${id}" data-author="${esc(author||'')}">${avatar(p,'avatar comment-avatar')}<div class="comment-main"><div class="bubble">${c.is_pinned?'<div class="pinned">📌 مثبت</div>':''}<div class="comment-name">${esc(name(p))}${ownerId&&author===ownerId?'<span class="owner">صاحب المنشور</span>':''}</div><div class="comment-text">${esc(c.body)}</div></div><div class="comment-meta"><span>${esc(time(c.created_at))}</span><button data-react="${id}" class="meta-btn ${mine?'active':''}">${mine?RX[mine][0]+' '+RX[mine][1]:'إعجاب'}</button><button data-reply="${id}" class="meta-btn">رد</button>${own?`<button data-edit="${id}" class="meta-btn">تعديل</button>`:''}${canDelete?`<button data-delete="${id}" class="meta-btn danger">حذف</button>`:''}${total?`<button data-likers="${id}" class="reaction-count">${reactionSummary(d)} ${total}</button>`:''}</div>${!reply?`<button class="replies-toggle" data-replies="${id}">عرض الردود</button><div class="replies" data-replies-for="${id}"></div>`:''}</div></article>`}
async function loadComments(reset=true){if(busy||!postId)return;busy=true;const list=$('commentsList'),more=$('moreComments');if(reset){offset=0;list.innerHTML='<div class="loading">جاري تحميل التعليقات…</div>';more.hidden=true}const r=await sb.from('comments').select('id,post_id,author_id,body,created_at,edited_at,is_pinned,parent_comment_id').eq('post_id',postId).is('parent_comment_id',null).order('is_pinned',{ascending:false}).order('created_at',{ascending:false}).range(offset,offset+PAGE-1);if(r.error){list.innerHTML='<div class="empty">تعذر تحميل التعليقات. حاول مرة أخرى.</div>';busy=false;return}const rows=r.data||[],ps=await profiles(rows.map(uid)),rd=await reactionData(rows.map(x=>x.id));if(reset)list.innerHTML='';if(reset&&!rows.length)list.innerHTML='<div class="empty">لا توجد تعليقات بعد. كن أول من يكتب 👋</div>';rows.forEach(c=>list.insertAdjacentHTML('beforeend',html(c,ps.get(uid(c)),rd)));offset+=rows.length;$('commentsCount').textContent=String(offset);more.hidden=rows.length<PAGE;busy=false}
async function replies(id,b){if(b.dataset.busy)return;b.dataset.busy='1';const box=document.querySelector(`[data-replies-for="${CSS.escape(id)}"]`),off=Number(b.dataset.offset||0);const r=await sb.from('comments').select('id,post_id,author_id,body,created_at,edited_at,is_pinned,parent_comment_id').eq('post_id',postId).eq('parent_comment_id',id).order('created_at',{ascending:true}).range(off,off+REPLY_PAGE-1);if(r.error){delete b.dataset.busy;toast('تعذر تحميل الردود');return}const rows=r.data||[],ps=await profiles(rows.map(uid)),rd=await reactionData(rows.map(x=>x.id));rows.forEach(c=>box.insertAdjacentHTML('beforeend',html(c,ps.get(uid(c)),rd,true)));if(rows.length===REPLY_PAGE){b.dataset.offset=String(off+rows.length);b.textContent='عرض المزيد من الردود';delete b.dataset.busy}else b.remove()}
function picker(target,id){document.querySelector('.reaction-picker')?.remove();const p=document.createElement('div');p.className='reaction-picker';p.innerHTML=Object.entries(RX).map(([k,v])=>`<button type="button" data-rx="${k}" aria-label="${v[1]}">${v[0]}</button>`).join('');target.parentElement?.appendChild(p);p.addEventListener('click',e=>{const b=e.target.closest('[data-rx]');if(b)setReaction(id,b.dataset.rx)});}
async function setReaction(id,type){if(!user){toast('سجّل الدخول للتفاعل');return}document.querySelector('.reaction-picker')?.remove();const cur=await sb.from('comment_reactions').select('reaction_type').eq('comment_id',id).eq('user_id',user.id).maybeSingle();const old=cur.data?.reaction_type;const r=old===type?await sb.from('comment_reactions').delete().eq('comment_id',id).eq('user_id',user.id):await sb.from('comment_reactions').upsert({comment_id:id,user_id:user.id,reaction_type:type},{onConflict:'comment_id,user_id'});if(r.error){toast('تعذر تحديث التفاعل');return}const d=(await reactionData([id])).get(id)||{counts:{},mine:null},row=document.querySelector(`[data-id="${CSS.escape(id)}"]`),btn=row?.querySelector('[data-react]');if(btn){btn.textContent=d.mine?RX[d.mine][0]+' '+RX[d.mine][1]:'إعجاب';btn.classList.toggle('active',!!d.mine);let rc=row.querySelector('.reaction-count'),total=Object.values(d.counts).reduce((a,b)=>a+b,0);if(total){if(!rc){rc=document.createElement('button');rc.className='reaction-count';row.querySelector('.comment-meta').appendChild(rc)}rc.textContent=`${reactionSummary(d)} ${total}`}else rc?.remove()}}
async function send(parent=null){if(!user){toast('سجّل الدخول لكتابة تعليق');return}const input=parent?document.querySelector(`[data-reply-form="${CSS.escape(parent)}"] input`):$('commentInput'),body=input?.value.trim();if(!body)return;const form=input.closest('form'),btn=form?.querySelector('button[type="submit"]');if(btn)btn.disabled=true;input.disabled=true;const payload={post_id:postId,author_id:user.id,body};if(parent)payload.parent_comment_id=parent;const r=await sb.from('comments').insert(payload).select('*').single();input.disabled=false;if(btn)btn.disabled=false;if(r.error){toast('تعذر نشر التعليق');return}input.value='';if(parent){const box=document.querySelector(`[data-replies-for="${CSS.escape(parent)}"]`),ps=await profiles([user.id]),rd=await reactionData([r.data.id]);box?.insertAdjacentHTML('beforeend',html(r.data,ps.get(user.id)||{},rd,true));document.querySelector(`[data-reply-form="${CSS.escape(parent)}"]`)?.remove()}else{const ps=await profiles([user.id]),rd=await reactionData([r.data.id]);$('commentsList').querySelector('.empty')?.remove();$('commentsList').insertAdjacentHTML('afterbegin',html(r.data,ps.get(user.id)||{},rd));offset++;$('commentsCount').textContent=String(offset)}toast(parent?'تم نشر الرد':'تم نشر التعليق')}
function replyForm(row){document.querySelectorAll('.reply-form').forEach(x=>x.remove());const id=row.dataset.id,n=row.querySelector('.comment-name')?.textContent?.trim()||'المستخدم',f=document.createElement('form');f.className='reply-form';f.dataset.replyForm=id;f.innerHTML=`<input maxlength="2000" placeholder="الرد على ${esc(n)}…" autocomplete="off"><button type="button" data-cancel>×</button><button type="submit">إرسال</button>`;row.querySelector('.comment-main').appendChild(f);f.querySelector('input').focus()}
async function edit(id,row){if(!user||row.dataset.author!==user.id)return;const v=prompt('تعديل التعليق',row.querySelector('.comment-text')?.textContent||'');if(v===null||!v.trim())return;const r=await sb.from('comments').update({body:v.trim(),edited_at:new Date().toISOString()}).eq('id',id).eq('author_id',user.id);if(r.error){toast('تعذر التعديل');return}row.querySelector('.comment-text').textContent=v.trim();toast('تم تعديل التعليق')}
async function del(id,row){if(!user||!(row.dataset.author===user.id||ownerId===user.id)){toast('ليس لديك صلاحية');return}if(!confirm('حذف التعليق؟'))return;const r=await sb.from('comments').delete().eq('id',id);if(r.error){toast('تعذر الحذف');return}row.remove();toast('تم حذف التعليق')}
async function likers(id){const r=await sb.from('comment_reactions').select('user_id,reaction_type,created_at').eq('comment_id',id).order('created_at',{ascending:false}).limit(60);if(r.error||!r.data?.length){toast('لا توجد تفاعلات بعد');return}const ps=await profiles(r.data.map(x=>x.user_id));$('likersList').innerHTML=r.data.map(x=>`<div class="liker">${avatar(ps.get(x.user_id)||{})}<strong>${esc(name(ps.get(x.user_id)))}</strong><span>${RX[x.reaction_type]?.[0]||'👍'}</span></div>`).join('');$('likersModal').hidden=false}
function bind(){document.addEventListener('click',e=>{if(e.target.closest('.reaction-picker'))return;const b=e.target.closest('button');if(!b)return;if(b.id==='backBtn'){const back=sessionStorage.getItem('mada-comments-return');sessionStorage.removeItem('mada-comments-return');if(back)location.href=back;else history.back();return}if(b.id==='moreComments')return;if(b.dataset.react){picker(b,b.dataset.react);return}if(b.dataset.replies){replies(b.dataset.replies,b);return}if(b.dataset.reply){replyForm(b.closest('.comment'));return}if(b.dataset.cancel){b.closest('form')?.remove();return}if(b.dataset.likers){likers(b.dataset.likers);return}if(b.dataset.edit){edit(b.dataset.edit,b.closest('.comment'));return}if(b.dataset.delete){del(b.dataset.delete,b.closest('.comment'));return}if(b.id==='closeLikers'){$('likersModal').hidden=true;return}document.querySelector('.reaction-picker')?.remove()});document.addEventListener('submit',e=>{if(e.target.id==='commentForm'){e.preventDefault();send()}else if(e.target.matches('.reply-form')){e.preventDefault();send(e.target.dataset.replyForm)}})}
async function boot(){if(!postId){toast('المنشور غير صالح');return}sb=window.MADA_SUPABASE_CLIENT||window.sb;if(!sb){toast('تعذر الاتصال بالخادم');return}user=(await sb.auth.getSession()).data?.session?.user||null;await loadOwner();bind();$('moreComments').addEventListener('click',()=>loadComments(false));$('closeLikers').onclick=()=>$('likersModal').hidden=true;await loadComments(true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();